/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SearchResult } from '../../models/search-result';

export interface ApiSearchGet$Plain$Params {
  term?: string;
  type?: string;
}

export function apiSearchGet$Plain(http: HttpClient, rootUrl: string, params?: ApiSearchGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<SearchResult>> {
  const rb = new RequestBuilder(rootUrl, apiSearchGet$Plain.PATH, 'get');
  if (params) {
    rb.query('term', params.term, {});
    rb.query('type', params.type, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SearchResult>;
    })
  );
}

apiSearchGet$Plain.PATH = '/api/Search';
